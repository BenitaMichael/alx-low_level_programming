#include <stdio.h>
#include "main.h"

/**
 * main - Entry point - prints name of file follow
 * by new line
 * @argc: number of command line arguments
 * @argv: array that contains the program command line arguments
 * Return: (0) if successful
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
